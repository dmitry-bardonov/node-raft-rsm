# ADR 0007: Fixed membership MVP

Status: accepted.

Only unique 1-, 3-, and 5-member voter sets are accepted. Dynamic membership without joint consensus is unsafe, so learners, membership changes, and automatic scaling remain absent from the API.
